
NOTE:  apache httpd.conf  section now covers the httpd.conf modifications.  -- 2026-02-20

try playing with the sudo --user=comptonpeslonline.com  in  pm2-initialze.bsh  and remove extra entries in the visudo

now automated.......   ###  FIRST do visudo and authorize the comptonpeslonline to get system information -- 2026-02-21

pm2-initialize.bsh will need to be ran AFTER the scripts have been copied over.


    ## initialize apache for pm2 usage
bash  -vx  pm2-apache-initialize.bsh   >  pm2-apache-initialize.bsh.log   2>&1  ;

   ##  dnf install node
bash  -vx  node-js.bsh                 >  node-js.bsh.log                 2>&1  ;

bash -vx   pm2-install.bsh             >  pm2-install.bsh.log             2>&1  ;

## run this LAST to create pm2 jobs and get them to start after reboot
bash -vx   pm2-initialize.bsh          >  pm2-initialize.bsh.log          2>&1 ;

which pm2 ; 
/bin/pm2  --version  ;

exit ;


###  cd   /home/comptonpeslonline.com/public_html/comptonPractice/homeworkAssignment/feathersServer ;
###  npm  update    ;   ##   for the feathers server
